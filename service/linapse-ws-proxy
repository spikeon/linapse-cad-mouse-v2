#!/usr/bin/env python3
"""Standalone entry point for the Linapse browser CAD bridge (debug / manual use)."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

from linapse.browser_bridge import main

if __name__ == "__main__":
    main()
